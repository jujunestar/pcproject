# PLAN — TracePC 2차 실습

## 오늘의 성공

배포된 URL에서 연결 코드로 이어진 Windows Agent의 실제 CPU 측정 데이터를 바탕으로 **"CPU 과부하 원인 후보 + 관련 프로세스 + 측정 근거"**가 표시된다.

## 스켈레톤

웹이 연결 코드를 발급한다 → Agent가 코드를 입력하고 CPU 데이터를 업로드한다 → 웹이 [성능 분석] 클릭 시 그 데이터를 읽어 화면에 표시한다.

## 오늘 만들 기능

1. 사용자가 웹에서 연결 코드를 발급받아 Agent를 자신의 세션에 연결할 수 있다.
2. 사용자가 PyInstaller로 빌드한 Agent를 실행해 CPU 사용률을 측정하고 Vercel API로 업로드할 수 있다.
3. 사용자가 웹에서 [성능 분석]을 눌러 CPU 과부하 원인 후보와 관련 프로세스 + 측정 근거를 확인할 수 있다.

## 완료 조건

**기능 1**
- 웹 화면에 일회용 연결 코드가 발급되어 표시된다.
- Agent에 코드를 입력하면 연결 성공 상태가 Agent 콘솔에 표시된다.
- 같은 코드로 연결된 세션임을 웹에서 확인할 수 있다.

**기능 2**
- 빌드된 exe를 더블클릭하면 Agent가 실행된다.
- Agent가 psutil로 전체 CPU 사용률을 주기적으로 측정해 API로 전송한다.
- 업로드 성공/실패가 Agent 콘솔에 로그로 표시된다.

**기능 3**
- [성능 분석] 클릭 시 웹 화면에 분석 결과가 표시된다.
- CPU 사용률 90% 이상이 5초 이상 지속된 경우 "CPU 과부하 원인 후보"라는 문구가 표시된다.
- 해당 구간 CPU 사용량이 가장 높은 프로세스 이름과 수치가 근거로 함께 표시된다.

## 오늘 안 만들 것

- RAM 분석
- Disk 분석
- 로그인 / 회원가입
- 프로세스 자동 종료
- 파일 자동 삭제
- Windows 설정 자동 변경
- 다크모드
- GPU / 네트워크 / CPU 온도 분석

## 걱정되는 것

연결 코드와 CPU 측정 데이터를 어떻게 저장해야 새로고침 후나 다른 기기에서 접속해도 유지되는지가 가장 불확실하다. Vercel Route Handler는 서버리스 환경이라 인스턴스 간 메모리가 공유되지 않을 수 있으므로, 서버 메모리 저장만으로 미리 확정하지 않는다. 스켈레톤 단계에서 새로고침과 다른 기기 접속 시에도 데이터가 유지되는지를 먼저 검증하고, 그 결과에 따라 최소한의 영속 저장 방식을 결정한다.

## 용어 변경

기존 문서에서 사용자 액션 버튼을 가리키던 "[지금 느려요]"라는 명칭은 앞으로 사용하지 않는다. 이후 신규 문서와 구현에서는 동일한 사용자 액션을 "[성능 분석]"으로 통일해 부른다. 이미 완료된 슬라이스의 spec(`specs/cpu-agent.md`, `specs/cpu-web-display.md`)과 PRD.md, SESSION.md는 과거 기록이므로 수정하지 않았다.
