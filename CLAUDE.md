# 프로젝트

TracePC는 Windows PC가 갑자기 느려지거나 멈췄을 때 시스템 데이터를
분석하여 성능 저하의 원인 후보, 측정 근거, 해결 방법을 보여주는
서비스다.

대상 사용자는 PC 성능 문제의 원인을 직접 판단하기 어려운 일반 사용자다.

지금은 2주 MVP이며 프로덕션 서비스가 아니다.

# 실행

-   웹 개발: `npm run dev`
-   웹 테스트: `npm test`
-   타입 검사: `npm run typecheck`
-   Windows Agent 실행: `python agent/main.py`

실제 프로젝트 설정 과정에서 명령어가 달라지면 이 문서를 함께 수정한다.

# 스택

-   Web: Next.js + TypeScript
-   API: Next.js Route Handler
-   Windows Agent: Python + psutil
-   Agent Packaging: PyInstaller
-   Deployment: Vercel
-   Database: MVP 초기에는 사용하지 않음

기술 스택을 임의로 변경하지 않는다. 새 의존성이 필요하면 추가하기 전에
이유를 설명한다.

# 규칙

-   기능 단위로 하나씩 구현한다.
-   커밋은 기능 단위 하나씩 한다.
-   한 번에 여러 기능을 추가하지 않는다.
-   TypeScript에서 `any`를 사용하지 않는다.
-   오류를 숨기거나 테스트를 우회하지 않는다.
-   요청하지 않은 리팩토링을 하지 않는다.
-   주석은 코드가 무엇을 하는지가 아니라 왜 그렇게 했는지가 필요할 때만
    작성한다.
-   선택지가 3개 이상이거나 요구사항이 불명확하면 임의로 결정하지 말고
    질문한다.

# 이 프로젝트의 암묵지

-   측정하지 않은 원인을 단정하지 않는다. 근거가 부족하면
    `원인을 확인할 수 없음`으로 표시한다.
-   사용자의 PC 상태를 임의로 변경하지 않는다. 프로세스 종료, 파일 삭제,
    Windows 설정 변경을 자동 실행하지 않는다.
-   모든 진단 결과에는 실제 측정 근거를 함께 보여준다. 예를 들어
    `RAM 부족`만 표시하지 않고
    `RAM 사용률 94%, chrome.exe 4.8GB 사용`처럼 판단 근거를 함께
    표시한다.

# 하지 말 것

-   자동 프로세스 종료
-   파일 자동 삭제
-   Windows 설정 자동 변경
-   측정되지 않은 원인의 추측
-   요청하지 않은 기능 추가
-   요청하지 않은 대규모 리팩토링
-   테스트를 skip 처리하거나 실패를 숨겨서 완료 처리
-   macOS / Linux 지원 추가
-   GPU / 네트워크 / CPU 온도 분석 추가
-   로그인 / 회원가입 / 결제 / 관리자 / 알림 / 다크모드 추가
