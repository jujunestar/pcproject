# PRD --- TracePC: PC 성능 저하 원인분석 서비스

## 한 문장

대학생이 Windows PC를 사용하다 갑자기 느려지거나 멈췄을 때 원인을 알지
못해, PC의 시스템 상태를 수집·분석하여 성능 저하의 원인 후보와 근거,
해결 방법을 확인할 수 있게 한다.

## 사용자

**김주하**

학교에서 노트북으로 코딩, 과제, 웹 브라우징 등 여러 작업을 하며, PC가
갑자기 느려지거나 멈추는 상황을 경험한다.

## 대체재

현재는 PC가 느려지면 작업관리자를 확인하거나, 실행 중인 프로그램을
종료하거나, 재부팅하거나, 인터넷에서 증상과 해결 방법을 검색한다.

하지만 성능 저하가 발생한 당시 어떤 시스템 자원과 프로세스가 문제와
관련됐는지 확인하기 어렵다.

## 쐐기

2주 안에 다음 한 흐름을 완성한다.

TracePC 웹사이트 접속\
→ Windows TracePC Agent 실행\
→ CPU / RAM / Disk / 프로세스 상태 수집\
→ PC가 느려졌을 때 **\[지금 느려요\]** 클릭\
→ 최근 시스템 상태 분석\
→ 병목 원인 후보 판별\
→ **관련 프로세스 + 측정 근거 + 해결 방법 표시**

MVP 분석 대상은 **CPU / RAM / Disk**로 제한한다.

## 성공 판정

Day 14 데모에서 다른 Windows PC에서 TracePC Agent를 실행하고 의도적으로
시스템 부하를 발생시킨 뒤 **\[지금 느려요\]**를 누르면 웹 화면에 **성능
저하 원인 후보 + 관련 프로세스 + 측정 근거 + 해결 방법**이 보인다.

## 안 만들 것

-   로그인 / 회원가입
-   결제
-   관리자 페이지
-   알림
-   다크모드
-   macOS / Linux 지원
-   GPU 분석
-   네트워크 분석
-   CPU 온도 분석
-   하드웨어 고장 진단
-   프로세스 자동 종료
-   파일 자동 삭제
-   Windows 설정 자동 변경
-   PC 문제의 100% 원인 확정
-   모든 종류의 PC 문제를 해결하는 범용 진단

## 암묵지

1.  **측정하지 않은 원인을 단정하지 않는다.** 충분한 근거가 없으면
    `원인을 확인할 수 없음`으로 표시한다.
2.  **사용자의 PC를 임의로 변경하지 않는다.** TracePC는 진단과 해결 방법
    제시까지만 수행하며 프로그램 종료, 파일 삭제, 설정 변경을 자동
    실행하지 않는다.
3.  **모든 진단 결과에는 실제 측정 근거를 함께 보여준다.** 예를 들어
    `RAM 부족`만 표시하지 않고
    `RAM 사용률 94%, chrome.exe 4.8GB 사용`처럼 판단 근거를 함께
    표시한다.
